name: chalet-to-go

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: chalet-to-go-webdocs:local
    restart: unless-stopped
    init: true
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:${APP_PORT:-8000}:8000"
    networks:
      proxy:
        aliases:
          - chalet-web
    environment:
      SITE_URL: "${SITE_URL:-https://www.chalettogo.com}"
      SITE_INDEXABLE: "${SITE_INDEXABLE:-true}"
      WHATSAPP_NUMBER: "${WHATSAPP_NUMBER:-5538998840910}"
      TRUST_COUNTRY_HEADER: "${TRUST_COUNTRY_HEADER:-false}"
      FORWARDED_ALLOW_IPS: "${FORWARDED_ALLOW_IPS:-127.0.0.1}"
    read_only: true
    volumes:
      - ./app/virtual_cards:/app/app/virtual_cards:ro
    tmpfs:
      - /tmp:size=64m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  proxy:
    name: ${PROXY_NETWORK:-nginxproxymanager_default}
    external: true
